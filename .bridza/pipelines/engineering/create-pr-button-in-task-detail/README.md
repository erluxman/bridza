# ✅ Create PR button in task detail alongside merge with main.

`bridza/engineering/create-pr-button-in-task-detail`  ·  pipeline `engineering`

`████████████████`  **3/3** stages  ·  14m 30s tracked  ·  🎉 all stages done

## At a glance

| # | Stage | Status | Time | Output | Runs |
|--:|-------|--------|------|--------|-----:|
| 1 | spec | ✅ done | 1m 11s | 2 files | 1 |
| 2 | build | ✅ done | 9m 52s | 4 files | 3 |
| 3 | review | ✅ done | 3m 27s | 1 file | 1 |

## The story

### 1. spec — ✅ done · 1m 11s

**Asked** — Task: Create PR button in task detail alongside merge with main. along with merge to main button there should be a button called create PR if the github cli is already there then that button will directly create a PR if there it is there is not it will open that creates a pr link in a browser.

**Output** — `acceptance.md`, `spec.md`

<sub>opencode · session `ses_f45731d54ffeBEVgPBobA0GWCq` · exit 0</sub>

### 2. build — ✅ done · 9m 52s · 3 runs

**Asked** — Task: Create PR button in task detail alongside merge with main. along with merge to main button there should be a button called create PR if the github cli is already there then that button will directly create a PR if there it is there is not it will open that creates a pr link in a browser. Fit c

**Output** — `server/bridge.js`, `server/bridza-run.js`, `src/app/api/client.js`, `src/app/features/task.jsx`

<sub>opencode · session `ses_f45502789ffe6FY3KqWNQjY4Tq` · exit 0</sub>

### 3. review — ✅ done · 3m 27s

**Asked** — Task: Create PR button in task detail alongside merge with main. along with merge to main button there should be a button called create PR if the github cli is already there then that button will directly create a PR if there it is there is not it will open that creates a pr link in a browser. Merge

**Output** — `review.md`

<sub>opencode · session `ses_f454715b4ffevZ0nfG0idxTkj8` · exit 0</sub>

---

<sub>✅ done · 🔄 running · ⏳ pending · ❌ failed — auto-generated from `metadata.json`, the same data Bridza shows.</sub>
