# When app is refreshed or page is refreshed, the sessions with agents should preserve, should continue. It should not be…

When the app refreshes the running... Do the running processes like cloud prompts etc get preserved? If it does not then it should like just refreshing a page should not kill all the process that were forged or like the communication with the terminal that is happening.
 when we select an agent.
