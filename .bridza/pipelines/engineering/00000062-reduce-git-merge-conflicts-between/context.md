# Reduce git merge conflicts between talks due to common files.

I see ECU during merging of ref.json or file that every ECU touches. There should be separation of concern that not every ticket should be touching the same file. They should be as independent as possible. So we have to organize the files in that manner.
 Thank you.
