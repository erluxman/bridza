# When merging to main does not happen without conflict, merge it with an AIS and like Claude or OpenCode and don't just…

When I press to merge to make it should try to merge with an AI agent rather than hard commands when the merge isnt simple with conventionall commands.  That way it will be easy to merge and more
 practical to merge based on the requirements and guidelines of merging. That way we will even be able to merge multiple commands into one using an AI hazard without having to go hardcoded objects.

## ARCHIVED by #88 — covered by [[conflict-auto-union-bridza-state]] (removes the common .bridza state conflicts) and [[conflict-open-in-vscode]] (hand-off for the rest). The AI-resolution idea it asked for is specced in `ai-assisted-conflict-merge`, archived as deferred.
