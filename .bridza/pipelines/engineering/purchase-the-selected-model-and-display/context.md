# Purchase the selected model and display it to the user for a step.

When I select AI agent and a model for a certain step on a flow, it does not persist or it does not show the persistence to the UI. So it's confusing which model that particular step will use. It should persist, use that model when it's executing that step.
 But I should be able to select which model that step will take.
