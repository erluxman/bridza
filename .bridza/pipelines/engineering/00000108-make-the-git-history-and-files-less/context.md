# Make the git history and files less noisy so that we can see the details when we need to but the noise won't be…

Alright, what I noticed is that there are too many files and there are too many commits. It should be lean. So what my idea is, every feature once I complete it will be one commit, one additional file. If it needs output files then fine, but they won't be committed. Or like demo resources, they won't be committed.
 So in JSON, it should have every single configuration of that particular task detail first. Second, it should have all the timeline of what the user did from start to end to reach to the current state of that task detail. There should be starting document, there should be current document structure and all the steps
 that I took in between from starting the state of the tax detail to the existing state of the tax detail. What changes in every single state? And in those intermediate state, if it changed some field of that document only record the fields that
 changed only the dips because of that particular action, not the whole document itself.
