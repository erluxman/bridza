# should give me output or run the application in that branch separately so that I can test it before I merge it.

Alright, every piece of work, if it changes the code base, should run this application in a different port than the currently running app. At least that branch should be running in a different branch so that I can take
 the test it before I can merge it. Alright, make that feature.

Every feature that changes the codebase should run that particular branch in a different port or whatever port is available so that I can test it before I merge it. If that is the case with WaveApps, if it touches other kind of applications like mobile applications or other file outputs, then files can be opened, not a problem.  That is not Wave. Basically, it should generate something as output for that particular branch, which obviously won't be committed if it's not something that is not the part of final merge because it could be intermediate APK file or something.  the output that I can test. 


And all these things should be a part of the flow itself. Maybe we need to add these fields on the flow itself. Each flow should be updated. If we need to update the different kind of flow session, we need to update them as well. But I want to test what I will see.
